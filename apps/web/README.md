# Visitor Web (apps/web)

React + TypeScript (Vite). VIS-1 scaffolding: placeholder that pings the API.

## Local
```bash
npm install
cp ../../.env.example .env   # sets VITE_API_BASE_URL
npm run dev   # http://localhost:5173
```

## Quality
```bash
npm run lint
npm run build
```
