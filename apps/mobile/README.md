# Visitor Mobile (apps/mobile)

React Native + TypeScript (Android + iOS). VIS-1 scaffolding: placeholder
screen that pings the API.

## Local
```bash
npm install
npm start          # Metro bundler
npm run android    # or: npm run ios
```
Set `API_BASE_URL` to your local API (default http://localhost:8000).
